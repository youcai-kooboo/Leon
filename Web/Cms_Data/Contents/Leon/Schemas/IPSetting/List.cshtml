<h3>IPSetting</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["FilterScope"], "IPSetting/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>
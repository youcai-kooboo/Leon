<h3>HtmlBanner</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["Site"], "HtmlBanner/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>
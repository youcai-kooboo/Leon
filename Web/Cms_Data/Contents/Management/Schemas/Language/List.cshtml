<h3>Language</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["LanguageName"], "Language/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>
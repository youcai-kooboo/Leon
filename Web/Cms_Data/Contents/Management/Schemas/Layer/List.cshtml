<h3>Layer</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["Title"], "Layer/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>
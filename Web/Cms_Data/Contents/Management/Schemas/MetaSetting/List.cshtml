<h3>MetaSetting</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["Layout"], "MetaSetting/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>
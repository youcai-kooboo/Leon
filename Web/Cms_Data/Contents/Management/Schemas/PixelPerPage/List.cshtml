<h3>PixelPerPage</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["PageName"], "PixelPerPage/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>
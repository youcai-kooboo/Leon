<h3>RedirectBasedOnBrowserLanguage</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["BrowserLanguage"], "RedirectBasedOnBrowserLanguage/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>
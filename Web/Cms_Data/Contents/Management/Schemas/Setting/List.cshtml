<h3>Setting</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["Key"], "Setting/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>
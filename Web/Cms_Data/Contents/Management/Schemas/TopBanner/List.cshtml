<h3>TopBanner</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["Title"], "TopBanner/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>